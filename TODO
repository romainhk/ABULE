=== CSS
* bordure de chaque coté du site => 4% (à valider)

* agrandir les images quand on clique dessus => première idée simpliste (css hover)
** utiliser plutôt "lightbox"

* séparer les style sur plusieurs feuilles :
** une générique : style.css
** une telle qu'on le veux (css3.0) : styleplus.css
** une légère pour IE et navigateurs sans css3 : stylemoins.css
*** sans dégradé, images jpg, sans horloge flash

=== PHP/js
* conversion de l'index en php ; contenu (en varchar?) dans la bdd
** stockage des images à part en blob ( table "image"-(nom, blob) )

* vérifier que le navigateur web supporte css 3 :
*... get_browser() -> browscat externe à spécifier dans le php.ini : http://browsers.garykeith.com/downloads.asp
*... sinon, parser $_SERVER['HTTP_USER_AGENT']
** ajouter un message d'avertissement (en bas ?)

* ABULE + titre de chaque page

* page php static ou dynamique ? (au pire : bench)

* mode admin : permet de compléter une page (session)
** éditeur intégré
** lien "éditer la page" à ajouter
** permettre l'ajout et la suppression de page (menu dynamique)

* lowsrc sur les images ? => js

* menu à droite : contact, news, rss => quels news, quels flux ?

=== Autre
* compléter la LICENCE et les méta-tags => (à valider)

